# The OpenJFX 0.1.0 run task accesses SourceSet state at execution time and is
# not compatible with Gradle's configuration cache. Build caching remains on.
org.gradle.configuration-cache=false
org.gradle.caching=true
