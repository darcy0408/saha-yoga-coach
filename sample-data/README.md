# Sample data

The application currently generates normalized synthetic landmarks in memory. No image of a person is required. Future prerecorded fixtures must contain landmark coordinates only, use consented or synthetic sources, and must not include faces or video.
